---
- name: sample play
  hosts: all
  tasks:
  - shell: id
  - shell: hostname
  - name: This is debug to print
    debug:
      msg: "GTR"

  - name: create user
    user:
      name: rico
      state: present
